Request #20028.

Write a function that outputs a string to the standard output.  Then save
an image using Shell.saveImage.  Start MLWorks with the saved image and
run the function.  This used to cause a segmentation violation.


---------------

Request #20023

Save an image by typing 'Shell.saveImage (<filename>, false);' into a
Listener.  Restart MLWorks, passing in the new saved image  and the 
File->Exit button should be enabled, whereas previously it wasn't.


---------------

Request #30284 (16/10/97)  Sessions saved from the GUI don't restart.

Save a session from the GUI, either by using the Shell.saveImage command or
by selecting File->SaveSession.  The saved session should run successfully,
whereas before it would give some warning messages and die.