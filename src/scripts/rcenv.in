# rc environment initialization for MLWorks
# Copyright (C) 1993 Harlequin Ltd

PATH=$SCRIPTDIR:$BINDIR:$PATH
LD_LIBRARY_PATH=$BINDIR:$LD_LIBRARY_PATH
if(~ $MANPATH ()) MANPATH=/usr/man:/usr/share/man
MANPATH=$MANDIR:$MANPATH
