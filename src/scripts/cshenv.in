# C shell environment initialization for MLWorks
# Copyright (C) 1993 Harlequin Ltd

setenv PATH "$SCRIPTDIR:$BINDIR:$PATH"
setenv LD_LIBRARY_PATH "$BINDIR:$LD_LIBRARY_PATH"
test -n "$MANPATH" || setenv MANPATH "/usr/man:/usr/share/man"
setenv MANPATH "$MANDIR:$MANPATH"
